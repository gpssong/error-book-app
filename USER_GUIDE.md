# 错题本 App 使用说明书

> 多子女错题本应用，让孩子的错题自动拍照、AI 识别、AI 讲解、归纳整理、打印复习。

---

## 一、5 分钟上手

### 1. 安装 App（手机）
下载 `error-book-v37-ipv4-fallback.apk` 到手机侧载安装。打开后进入登录页。

### 2. 注册账号
首次使用点击 **「注册」**，填用户名、邮箱、密码（≥6 位），注册即登录。

> ⚠️ 每个账号数据完全独立。一个账号可以管理多个孩子档案，但不同账号之间看不到彼此的数据。

### 3. 添加孩子
登录后首页会提示「添加你的第一个孩子」。点击 **「添加孩子」**，填孩子昵称、年级（小学/初中/高中等）、选个头像和颜色，保存。

### 4. 拍照录入第一道错题
首页底部 **「拍题」** → 拍照或从相册选图 → 在照片上**圈出题目区域**（一次可圈多道题）→ 点 **「识别」** → AI 自动识别公式、生成题目内容 → 一键保存入库。

完成！下面看每个功能怎么用。

---

## 二、核心功能详解

### 📸 拍题录入（CameraScreen）

**两种入口**：
- 首页底部 **「拍题」** 按钮
- 错题详情页右上角的 **「+」** 按钮

**操作步骤**：
1. **选择图片来源**：系统相机 / 相册（横屏试卷也能拍）
2. **圈选题目区域**：用手指在照片上画矩形框住每道题；可拖拽 8 个手柄调整大小，可加多个框
3. **去手写开关**（可选）：如果孩子已经在原题上做了笔记想从干净版开始识别，打开 **「去手写」**
4. **选择学科**：数学 / 语文 / 英语 / 物理 / 化学 / 生物
5. 点 **「识别」**，3-10 秒返回识别结果：
   - 标题（自动）
   - 知识点（自动）
   - 题目原文（含 LaTeX 公式正确渲染）
6. **手动微调**（可选）：识别有误差时直接改文字内容
7. 点 **「保存入库」**，错题出现在对应孩子的错题列表里

**批量多题识别**：
一次拍照多道题，分别框选后按顺序一次识别，每题独立入库。某题识别失败不影响其他题。

---

### 🤖 AI 讲解（错题详情 → AI 讲解）

在任意错题详情页，点击 **「AI 讲解」** Tab → 点 **「开始讲解」**，AI 会给出三段内容：

| 内容 | 作用 |
|---|---|
| **错误原因** | 指出孩子可能的思维误区 |
| **知识点讲解** | 把题目涉及的知识点系统讲一遍 |
| **分步教程** | 一步步演示怎么正确解题 |

讲解内容**带 LaTeX 公式渲染**（数学符号、分数、希腊字母、根号等都正常显示）。

可点 **「保存讲解」** 永久存到错题记录里，下次直接看不用再问 AI。

---

### 📚 同类练习（错题详情 → 同类练习）

按当前错题的知识点，**自动生成 8 道同类练习题**（不重复），含参考答案，可一键打印给孩子做。

---

### 🎯 AI 练习（首页底部 → AI 练习）

不针对某道错题，而是按 **学科** 一键生成 8 道随机练习题：
- 选科目 → 点 **「出题」**
- 每天可出 3 组（免费版），Pro/Family 不限
- 每题可展开/收起答案
- 一键 **「打印练习」** 出来给孩子当作业

---

### 📋 错题管理

**错题列表**（首页底部 → 错题）：
- 按学科过滤、按日期排序
- 多选模式：勾选多道 → 顶部出现 **「打印」** 和 **「删除」** 按钮
- 点单道 → 进入详情

**错题详情 3 个 Tab**：
1. **题目详情**：查看原图、LaTeX 渲染的题目、**手写批注**（直接在屏幕上写字，覆盖在原图上）
2. **AI 讲解**：上面已介绍
3. **同类练习**：上面已介绍

**手写批注**：
- 详情页点 **「批注」** → 进入画布
- 选笔粗细 / 颜色 / 橡皮
- 写完自动保存为 SVG 叠加层，**不影响原图**
- 不想用了可以 **「清除手写」**，原图原封不动

---

### 🖨️ 打印

任意地方看到 **「打印」** 按钮即可打印：

| 入口 | 打印内容 |
|---|---|
| 错题列表（多选） | 多道错题卡，A4 一张一道 |
| 错题详情（单选） | 当前错题卡一张 |
| AI 练习 | 随机练习题 8 道一张 |

**打印预览功能**：
- A4 纸张大小自适应
- 顶部可改 **「2 列布局 / 单列布局」**
- **「含参考答案」** 开关：默认关闭，需要答案时打开
- **底部 4 个 tab 不会打印出来**（专门处理过）
- 手机端调起系统打印面板（App 内走 Android 原生 PrintManager），可保存 PDF 或连打印机

---

### 👶 多孩子管理

**我的页面 → 孩子管理**：
- 增 / 删 / 编辑孩子档案
- 删除孩子会**级联删除该孩子的所有错题**（不可恢复，谨慎）
- 首页左上角点孩子头像可**快速切换**孩子
- 不同孩子错题数据完全隔离

---

### 💎 订阅套餐

| 套餐 | 价格 | 适合 | 额度 |
|---|---|---|---|
| **免费版** | ¥0 | 偶尔用 | OCR 10 次/天 · AI 讲解 3 次/天 · 同类题 3 组/天 · 1 个孩子 |
| **Pro 个人版** | ¥18/月 或 ¥148/年 | 单娃主力 | 全部无限 + 打印 30 张/月 + 手写高级工具 |
| **Family 家庭版** | ¥28/月 或 ¥268/年 | 多娃家庭 | 全部无限 + 5 个孩子 |

**升级方式**：我的页面 → **「升级 Pro / 升级 Family」** → 扫码支付 → 联系客服手动激活（7 天内有效，过期失效）。

---

### 🛡️ 管理员功能（仅 gpssong）

管理员账号登录后，**我的页面**会出现 **「账号管理」** 入口：
- 查看所有注册用户
- 修改任意用户的套餐类型、到期时间
- 一键 **「重置每日额度」**

`/config.html` 配置管理页：管理 OCR / AI 的所有 API Keys、账号列表（多套配置切换）。

---

## 三、常见问题

### Q1：手机连家里 Wi-Fi 打不开 App，但用流量可以？
**已修复（v37）**。App 现在会自动尝试 4 个访问地址（外网域名 IPv6 → 公网 IPv4 → 内网 IP → 直连后端），4 秒内谁先通就用谁。如果还不行：
1. 检查 Wi-Fi 路由器是否开启 AP 隔离
2. 临时用流量访问
3. 内网 Wi-Fi 下访问 `http://192.168.0.14:4040`（前提是和服务器同 Wi-Fi）

### Q2：识别错了怎么办？
两种修法：
- **轻度错误**：在录入页直接改文字内容再保存
- **识别失败（少见）**：录入页会显示「需手动补录」，手动输入题目和答案即可入库

### Q3：数学公式渲染不对？
已加入 LaTeX 后处理（`latexNormalize`），自动修复：
- `\sqrt 2` → `\sqrt{2}`
- 根号缺失大括号
- 反斜杠转义错误
- unicode 符号（≤ ≥ ∈ ∪）→ 标准 LaTeX

如果有渲染异常的题目，截图发我反馈，我加规则。

### Q4：错题数据存在哪里？
全部存在你自部署的 MongoDB 数据库里（服务器 `192.168.0.14`）。**App 卸载、换手机不会丢**，重新登录就看到全部数据。

### Q5：打印出来有空白/格式乱？
- 选「单列布局」内容少时也不会留白
- 选「2 列布局」适合短题密集打印
- 手机端打印预览打开「含参考答案」开关即可

### Q6：管理员账号是什么？
只有 `gpssong`。其他用户看不到管理入口。

---

## 四、配套服务

| 服务 | 用途 | 网址 |
|---|---|---|
| 主入口 | Web / App 都用 | `http://error.93gushi.com:4040` |
| 配置管理 | API Key / 账号管理 | 同上 + `/config.html` |
| 内网直连 | 同 Wi-Fi 用 | `http://192.168.0.14:4040` |
| 公网直连 | 无 IPv6 时用 | `http://220.187.13.231:4040` |

---

## 五、版本信息

- **当前版本**：v37（2026-09-06）
- **核心特性**：IPv4 fallback · DNS 双栈 · OCR 数学题视觉兜底 · LaTeX 渲染 · 多孩子档案 · 多用户隔离 · 付费墙
- **APK 位置**：`/Volumes/mac使用/错题本app/apk/error-book-v37-ipv4-fallback.apk`

---

需要新功能或发现 Bug 直接说，记忆会保留所有上下文。